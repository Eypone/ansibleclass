---
- name: Facts
  hosts: linuxservers

  tasks:
    - name: Debug Facts
      ansible.builtin.debug:
        msg: "{{ ansible_facts }}"

    - name: Debug Facts Hostname - Unsafe Text
      ansible.builtin.debug:
        msg: "{{ ansible_facts['nodename'] }} and {{ ansible_facts['nodename'] }}"
